#ifndef REPLYER__H
#define REPLYER__H

class Replyer{
public:
  void Replyer(string line);


  bool isM;
  string C;
  int B;
  vector<string> vS;
  static vector<string> ReadFile::ReadFile(string name);
  
};
#endif
